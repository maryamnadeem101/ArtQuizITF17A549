<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="application.assignment.quizbitf17a549">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:label="@string/app_name" android:name="Activity2"/>
        <activity android:label="@string/app_name" android:name="Activity3"/>
        <activity android:label="@string/app_name" android:name="Activity4"/>
        <activity android:label="@string/app_name" android:name="Activity5"/>
        <activity android:label="@string/app_name" android:name="Activity6"/>
        <activity android:label="@string/app_name" android:name="Activity7"/>
        <activity android:label="@string/app_name" android:name="Activity8"/>
        <activity android:label="@string/app_name" android:name="Activity9"/>
        <activity android:label="@string/app_name" android:name="Activity10"/>


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>